import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ecv-mobile-list-ionic',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
